<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Capteurs corporels : nécessaire pour détecter le sommeil -->
    <uses-permission android:name="android.permission.BODY_SENSORS" />
    <!-- Notification "surveillance en cours" sur Android 13+ -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <uses-feature android:name="android.hardware.type.watch" />

    <application
        android:label="@string/app_name"
        android:theme="@android:style/Theme.Material.NoActionBar">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Service de surveillance passive du sommeil (Health Services) -->
        <service
            android:name=".SleepPassiveService"
            android:exported="true"
            android:permission="com.google.android.gms.permission.BIND_PASSIVE_DATA_LISTENER">
            <intent-filter>
                <action android:name="androidx.health.PASSIVE_LISTENER" />
            </intent-filter>
        </service>

    </application>
</manifest>
